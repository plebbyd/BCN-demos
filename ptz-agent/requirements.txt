anthropic>=0.25.0
pyyaml>=6.0
openai>=1.0.0       # for vLLM / OpenAI-compat backend
requests>=2.31.0    # for Ollama backend
Pillow>=10.0.0      # image processing for simulated PTZ camera
flask>=3.0.0        # optional: Slack webhook listener (legacy)
slack-bolt>=1.18.0  # optional: Slack Socket Mode scheduler

# Optional — install for specific features:
# croniter>=1.3.0      # cron expression scheduling in tasks.yaml
# pyserial>=3.5        # serial/UART sensor plugins
# reolink_aio          # PTZ camera tool plugin (real hardware)

# Optional — MCP (Cursor / Claude): Simulated PTZ tools over stdio
# mcp>=1.0.0

# Optional — detection backends (install what you need):
ultralytics>=8.3.0   # YOLO object detection
# torch>=2.0.0         # required by BioCLIP
# open_clip_torch>=2.20.0  # BioCLIP biological classification
# torchvision>=0.15.0  # BioCLIP image preprocessing
# opencv-python>=4.8.0 # BioCLIP Grad-CAM localization
# numpy>=1.24.0        # BioCLIP + YOLO
# huggingface_hub>=0.20.0  # BioCLIP embedding downloads
# Gemma4 VLM (vision + text) uses Ollama HTTP API (ollama pull gemma4:31b …); requests is required (listed above).
# The same Gemma4 model is used for the MSA agent loop on edge devices when config/config.yaml backend is set to "ollama".
